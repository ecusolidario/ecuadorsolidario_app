-# Rails flash messages styled for Bootstrap 3.0
- flash.each do |name, msg|
  - if msg.is_a?(String)
    :javascript
      $.notify({message: '#{msg}'},{animate: {enter: 'animated zoomInDown',exit: 'animated zoomOutUp'},type: "#{name.to_s == 'notice' ? 'success' : 'danger'}"      });
